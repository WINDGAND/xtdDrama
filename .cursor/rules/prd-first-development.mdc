---
description: 开发必须遵循 PRD.md；冲突时提示并让用户选择
alwaysApply: true
---

# PRD 优先级与冲突处理（强制）

## 开发前置要求

- 每次开始任何“开发/改代码/改需求实现/改交互/改数据结构/改接口”等工作之前，必须先通读一遍 `PRD.md`，以确保当前任务与产品定义一致。
- 在实现过程中，以 `PRD.md` 的功能范围、交互约束、术语口径、优先级与验收标准为唯一真源（source of truth）。

## 当用户指令与 PRD 冲突时（必须执行）

如果用户的 prompt/需求与 `PRD.md` 存在冲突、缺失或相互矛盾：

- 明确指出**冲突点**（引用 PRD 对应章节/要点进行对照说明）
- 给出**两个可选项**让用户选择：
  - **选项 A（以 PRD 为准）**：保持 `PRD.md` 不变，按 PRD 的要求实现
  - **选项 B（以本次 prompt 为准）**：按用户本次要求改动实现，并同步提出需要如何更新 `PRD.md`（新增/修改哪些段落）以消除矛盾

在用户做出选择前，不要直接继续实现可能导致偏离 `PRD.md` 的改动。

## 补充约束

- 不允许“脑补需求”。`PRD.md` 未定义的部分，必须先按“冲突处理”流程澄清为准。
- 若同一功能在 `PRD.md` 与其他文档（如 `README.md`）存在不一致，默认以 `PRD.md` 为准，并在输出中提示需要统一口径。
